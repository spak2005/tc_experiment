export default function Home() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">Autonomous transaction coordination</p>
        <h1>Your AI TC gets an inbox and runs the file.</h1>
        <p className="lede">
          Sign up, get a named TC email, forward the executed contract, and the
          TC opens the file, maps the timeline, coordinates parties, and
          escalates deadline risk.
        </p>
      </section>
    </main>
  );
}
